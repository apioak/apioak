return {
    user     = require("apioak.db.user"),
    role     = require("apioak.db.role"),
    token    = require("apioak.db.token"),
    router   = require("apioak.db.router"),
    plugin   = require("apioak.db.plugin"),
    project  = require("apioak.db.project"),
    upstream = require("apioak.db.upstream"),
}
