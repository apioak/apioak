local _M = {
    type  = "Traffic Control",
    name  = "Limit Conn",
    desc  = "Add a limit conn to your APIs.",
    key   = "limit-coon",
    order = 1101
}

function _M.http_access(oak_ctx)

end

return _M
