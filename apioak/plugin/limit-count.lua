local _M = {
    type  = "Traffic Control",
    name  = "Limit Count",
    desc  = "Add a limit count to your APIs.",
    key   = "limit-count",
    order = 1102
}

function _M.http_access(oak_ctx)

end

return _M
